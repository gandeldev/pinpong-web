const playerOne = document.querySelector('.player.one')
const playerTwo = document.querySelector('.player.two')

function movePlayerOne() {
    if (event.keyCode == 87) {
        playerOne.style = height: ;
        playerOne.getPosition
    }
}
